name: CI (legacy)

on:
  workflow_dispatch:

jobs:
  notice:
    name: legacy-notice
    runs-on: ubuntu-latest
    steps:
      - name: Show migration notice
        run: echo "This workflow is deprecated. Use build.yml, server-test.yml, and client-test.yml."
